# include<iostream>
using namespace std;
int main(){  // Round bracket called perenthisis
    cout<<"Enter Positive Number : ";
    int n;
    cin>>n;
    if(n%2 == 0) cout<<"Number is Even";
    else if (n%2 != 0) cout<<"Number is Odd";
    return 0;
}