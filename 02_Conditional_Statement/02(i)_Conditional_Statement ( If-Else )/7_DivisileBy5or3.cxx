# include<iostream>
using namespace std;
int main(){
    cout<<"Enter Number : ";
    int n;
    cin>>n;
    // if( (n%5 == 0) || (n%3 == 0) ) cout<<n<<" is divisible by 5 or 3";
    // else cout<<n<<" is not divisible by 5 & 3";
    if(0 && 0) cout<<"Zero";
    else cout<<"One";
    return 0;
}